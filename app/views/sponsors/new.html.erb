<h1>NUEVO SPONSOR</h1>
<%= form_for @patrocinador do |f| %>
	<%= f.label :Empresa %> <br> 
	<%= f.text_field :name %>
	<%= f.label :Descripcion %> <br> 
	<%= f.text_area :description %>
	<%= f.file_field :avatar %> <br>
	<%= f.submit "Guardar" %> <br>
<% end %>